#include <simplecpp>
main_program{
int length = 5;
turtleSim();
repeat(100){
    left(36);
    forward(length);
    right(144);
    length = length +10;
    forward(length);
    right(36);
    length = length +10;


}
getClick();
}
