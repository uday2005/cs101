#include <simplecpp>

main_program{
    char grade;
    int score;
    cout << "Enter score (0 to 100)\n";

    //  Terniraray opertor 

    grade = (score >= 90) ? 'A':
            (score >= 80) ? 'B':
            ()


}
